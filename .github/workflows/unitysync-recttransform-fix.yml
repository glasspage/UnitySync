name: UnitySync patch test

on:
  push:
    branches:
      - fix/recttransform-canvas-live-create

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - name: Validate runner
        run: echo "runner ok"
